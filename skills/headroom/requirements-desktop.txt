# Optional desktop display; scoring and the web dashboard do not need these.
#
# Windows gets a system-tray card (pystray + pywebview + WebView2).
# macOS gets a native menu bar item built on PyObjC, which must be installed
# into a dedicated interpreter -- the recommended setup is:
#
#   python3 -m venv ~/.headroom/venv
#   ~/.headroom/venv/bin/pip install -r skills/headroom/requirements-desktop.txt
#
# headroom's hook then finds it automatically (override with HEADROOM_DESKTOP_PYTHON).
pystray==0.19.5; sys_platform == "win32"
pywebview==6.2.1; sys_platform == "win32"
pyobjc-framework-Cocoa>=10; sys_platform == "darwin"
Pillow>=10
