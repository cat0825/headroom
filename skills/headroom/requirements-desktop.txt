# Optional tray display (Windows notification area or macOS menu bar); scoring and the web UI do not need these.
#
# Windows gets a tray card (pystray + Pillow); the animated WebView2 card adds
# pywebview. macOS and Linux get the same tray card on Tk's main-thread event
# loop, which is what pystray's macOS backend requires.
#
# pystray on macOS needs PyObjC, and headroom's hook finds a dedicated
# interpreter automatically (HEADROOM_DESKTOP_PYTHON overrides):
#
#   python3 -m venv ~/.headroom/venv
#   ~/.headroom/venv/bin/pip install -r skills/headroom/requirements-desktop.txt
pystray==0.19.5
Pillow>=10
pywebview==6.2.1; sys_platform == "win32"
pyobjc-framework-Cocoa>=10; sys_platform == "darwin"
pyobjc-framework-Quartz>=10; sys_platform == "darwin"
